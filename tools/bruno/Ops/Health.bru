meta {
  name: Health (liveness)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/healthz
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.status: eq ok
}

docs {
  Liveness probe. Always 200 with process uptime; does not touch the DB.
}
