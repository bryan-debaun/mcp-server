{
    "servers": {
        "bryan-debaun-mcp": {
            "type": "http",
            "url": "https://bad-mcp.onrender.com/mcp",
            "headers": {
                "Authorization": "Bearer ${env:MCP_API_KEY}",
                "X-INTERNAL-KEY": "${env:MCP_INTERNAL_KEY}"
            }
        }
    }
}